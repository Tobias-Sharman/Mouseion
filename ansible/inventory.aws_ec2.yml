plugin: amazon.aws.aws_ec2
regions:
  - eu-west-2
filters:
  "tag:Project": cluster
keyed_groups:
  - key: ec2_tags.Name
    prefix: ""
    separator: ""
compose:
  ansible_host: public_ip_address | default(private_ip_address)
