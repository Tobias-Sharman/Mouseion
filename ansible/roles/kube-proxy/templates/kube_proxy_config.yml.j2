apiVersion: kubeproxy.config.k8s.io/v1alpha1
kind: KubeProxyConfiguration
clientConnection:
  kubeconfig: "{{ kube_config_dir }}/kube-proxy.kubeconfig"
mode: "iptables"
clusterCIDR: "{{ pod_cidr }}"
hostnameOverride: "{{ ec2_tags.Name }}"
